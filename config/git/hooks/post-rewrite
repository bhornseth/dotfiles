#!/usr/bin/env bash
hook_dir="$(dirname "${BASH_SOURCE[0]}")"
hook_name="$(basename "${BASH_SOURCE[0]}")"

helper="$hook_dir/refresh-ctags"
[[ -x "$helper" ]] && "$helper" "$@" || true

runner="$hook_dir/run-local-hook"
[[ -x "$runner" ]] && "$runner" "$hook_name" "${BASH_SOURCE[0]}" "$@" || true
